# Northcoders News API

# ENV set-up

# Install it as a dev dependency
npm install dotenv 
# For using this project, you need to create two env files for your project: 
.env.test and .env.development.
# You can write your .env file using the following example:
PGDATABASE=database_name_here
